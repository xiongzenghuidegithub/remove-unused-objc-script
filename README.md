# remove-unused-objc-script

A script which scans a directory, creates an array of objc classe and does a second iteration, removing any that are unreferenced.

Was made for a large legacy project with a lot of unused code paths and classes.
